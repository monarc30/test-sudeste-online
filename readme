Algumas instruções:

A documentação do projeto foi gerada utilizando o link laravel-apidoc-generator que respeita as regras do Swagger, o arquivo
JSON se encontra dentro da pasta public/docs/collection.json

Para acessar via browser por exemplo é:
http://localhost/seu-host/public/docs/#apidosagempdf

O PDF das dosagens é por exemplo assim:

http://localhost/api/DosagemPDF

A autenticação foi feita utilizando-se o OAUTH2 para gerar o Token Bearer.

Lembrando que utilizei o passport para gerar a chave no servidor.
